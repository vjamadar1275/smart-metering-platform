{
  "_comment": "Canonical per-environment config for the Bronze streaming pipeline. Mirrored into bundles/bronze_pipeline.yml's target.prod.variables by convention — see src/config/dev/bronze_pipeline.json for the full explanation.",
  "environment": "prod",
  "catalog_name": "smartmeter_prod",
  "event_hub": {
    "namespace": "<from-terraform-output:module.event_hub.namespace_name>",
    "name": "<from-terraform-output:module.event_hub.event_hub_name>",
    "consumer_group": "bronze-streaming",
    "partition_count": 200
  },
  "secrets": {
    "scope": "smartmeter-prod-secrets",
    "connection_string_key": "eventhub-connection-string",
    "key_vault_id": "<from-terraform-output:module.key_vault.key_vault_id>"
  },
  "bronze": {
    "max_offsets_per_trigger": 2000000,
    "note": "Sized for the current 10M-meter scale, with the pipeline's continuous trigger and Serverless autoscale absorbing bursts toward the 100M-meter design target without a config change."
  }
}
