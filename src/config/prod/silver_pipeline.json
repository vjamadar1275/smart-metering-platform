{
  "_comment": "Canonical per-environment config for the Silver pipeline + its nightly late-arrival reconciliation job. Mirrored into bundles/silver_pipeline.yml and bundles/silver_late_arrival_reconciliation_job.yml's target.prod.variables by convention — see src/config/dev/bronze_pipeline.json for the full explanation of this file's role.",
  "environment": "prod",
  "catalog_name": "smartmeter_prod",
  "silver": {
    "watermark_hours": 24,
    "max_clock_skew_hours": 1,
    "max_plausible_liters": 1000000000
  },
  "reconciliation": {
    "lookback_days": 3,
    "schedule_pause_status": "UNPAUSED",
    "note": "3-day lookback covers the 24h watermark plus slack for a delayed/retried prior run without re-scanning meaningfully more Bronze data than necessary."
  }
}
